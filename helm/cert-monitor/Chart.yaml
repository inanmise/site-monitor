apiVersion: v2
name: certmonitor-chart
description: SSL/TLS Certificate Monitoring System with alerting, escalation, and Kubernetes-native HA support.
type: application
version: 19.80.3
appVersion: "19.80.3"
keywords:
  - certificate
  - ssl
  - tls
  - monitoring
  - alerting
home: https://github.com/inanmise/certmonitor
sources:
  - https://github.com/inanmise/certmonitor
maintainers:
  - name: inanmise
    email: erdi.inanmis@gmail.com
# Bundled Bitnami postgresql subchart KALDIRILDI: hiçbir ortam onu enable etmiyordu
# (postgresql.enabled=false her yerde; prod dış/yönetilen DB, self-hosted için k8s/postgres.yaml var)
# ve Bitnami'nin legacy Helm repo'su (charts.bitnami.com/bitnami) kullanımdan kalkıp CI'da
# "helm dep update" adımını timeout'a düşürüyordu. Gerekirse ileride OCI kaynağıyla geri eklenebilir.
