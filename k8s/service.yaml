apiVersion: v1
kind: Service
metadata:
  name: cert-monitor
  namespace: cert-monitor
  labels:
    app: cert-monitor
spec:
  type: ClusterIP
  selector:
    app: cert-monitor
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
