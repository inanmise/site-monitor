apiVersion: v1
kind: Service
metadata:
  name: site-monitor
  namespace: site-monitor
  labels:
    app: site-monitor
spec:
  type: ClusterIP
  selector:
    app: site-monitor
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
